library ieee;
use ieee.std_logic_1164.all;
use ieee.numeric_std.all;

entity Contador is
	generic (
		CLOCK_FREQUENCY : real := 	50.0e6;
		W : natural := 32
	);
	port(
		CLOCK : in std_logic;
		RESET : in std_logic;
		COUNT : in std_logic_vector (w-1 downto 0);
		DONE : out std_logic
	);
end entity Contador;

architecture Main of Contador is
	constant CLOCK_PERIOD : time := 1.0 sec / CLOCK_FREQUENCY;
	signal count : unsigned
begin
	process
	begin
		if RESET = '1' then
			
	end process;